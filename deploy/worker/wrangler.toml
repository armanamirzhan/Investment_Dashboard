name = "finance-proxy"
main = "worker.js"
compatibility_date = "2024-09-23"

# The FMP_API_KEY is stored as a secret (not in this file).
# Set it with: wrangler secret put FMP_API_KEY
# Then paste your key when prompted.

# Optional: Custom domain route (uncomment after setting up)
# routes = [
#   { pattern = "lumentel.com/api/finance/*", zone_name = "lumentel.com" }
# ]
